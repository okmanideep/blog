{
  "name": "blog",
  "version": "1.0.0",
  "description": "okmanideep's blog",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy"
  },
  "type": "module",
  "author": "okmanideep",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0"
  },
  "dependencies": {
    "@11ty/eleventy-plugin-webc": "^0.12.0-beta.2",
    "@11ty/eleventy-upgrade-help": "^3.0.1",
    "clean-css": "^5.3.1"
  }
}
