<script webc:type="js">
  let tagLinks = tags.map(tag => `<a href="${site.baseurl}/tag/${tag}">${tag}</a>`).join(',')
  let html
  if (tags.size < 0) {
    html = ``
  } else {
    html = `on ${tagLinks}`
  }

  html
</script>
